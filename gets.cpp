#include <iostream>
#include <string>
#include <vector>

using namespace std;

int main(int argc, const char *argv[])
{
    char p[10],q[10];
    cin.get(p, 10);
    cin.get();
    cin.get(q, 10);
    cout << "p: " << p << endl;
    cout << "q: " << q << endl;
    return 0;
}
